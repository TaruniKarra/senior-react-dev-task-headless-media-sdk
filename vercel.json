{
  "buildCommand": "pnpm --filter @headless-media/core build && pnpm --filter @headless-media/wrappers build && pnpm --filter @headless-media/components build && pnpm --filter app build",
  "outputDirectory": "apps/app/dist",
  "installCommand": "npm install -g pnpm@11.5.1 && pnpm install --no-frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
